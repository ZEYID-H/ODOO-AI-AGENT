# Copy to .env.local (already git-ignored by the Next.js scaffold's own .gitignore).
# Must be prefixed NEXT_PUBLIC_ to be readable in the browser bundle.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
